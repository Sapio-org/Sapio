import 'package:flutter/material.dart';

class Interest {
  final String name; // Name or type of the interest (e.g., "Math", "AI")
  final IconData icon; // Icon to visually represent the interest

  Interest({
    required this.name,
    required this.icon,
  });
}
