import 'package:flutter/material.dart';

class Project {
  final String name; // Name or type of the interest (e.g., "Math", "AI")
  final String descr; // Icon to visually represent the interest

  Project({
    required this.name,
    required this.descr,
  });
}
